# Create a key in Cassis under Settings -> API keys. One key serves the CLI and the MCP server.
CASSIS_API_KEY=sk-k6-...
# Optional. Overrides the project recorded in cassis/project.yml after the first pull.
CASSIS_PROJECT_ID=
